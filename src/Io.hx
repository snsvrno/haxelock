function log(text : String) {
	Sys.println(text);
}

function error(text : String) {
	Sys.println("ERROR: " + text);
}

function trace(text : String) {
	Sys.println("TRACE: " + text);
}

function warn(text : String) {
	Sys.println("WARN: " + text);
}